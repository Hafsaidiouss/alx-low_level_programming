#include "main.h"
/**
 * add - adds two integers and returns the result
 * @c : first number
 * @b : second number
 * Return: the result
 */
int add(int c, int b)
{
	int sum;

	sum = c + b;
	return (sum);
}
